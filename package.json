{
  "name": "dte-public-proxy",
  "version": "1.0.0",
  "description": "CORS proxy for Days to Expiry dashboard → Public.com API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev":   "nodemon server.js"
  },
  "engines": { "node": ">=18.0.0" },
  "dependencies": {
    "cors":       "^2.8.5",
    "express":    "^4.18.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
